# Build Notes

This directory contains the Gate 1 CVPR 2026 paper skeleton for AP0006.

Build locally with:

```bash
cd paper/cvpr2026_ap0006_memory_modules
./build.sh
```

Expected outputs:

- `main.pdf`
- `page_count.txt`

Gate 1 only verifies the template, directory layout, and compilation. It does not write the final paper body.
