# Correct vs Wrong Memory Battle

This panel compares approved memory against distractor and control memories.

Important honest finding:

- approved object-patch memory selects `seed8`
- random crop memory also selects `seed8`
- same-frame non-road-sign crop also selects `seed8`

So this demo is useful for communication, but it also exposes that specificity is still limited.
