# Candidate Gallery / Memory Chooses

This demo shows the full `seed1..8` candidate pool rather than only a hand-picked pair.

- No-memory baseline selects `seed1`.
- Approved object memory selects `seed8`.
- The gallery makes the candidate-pool comparison explicit.
